local class = require('lib/middleclass')

local PlayerAnimator = class('PlayerAnimator')
-- this is going to require a state machine for the player
-- 


function getAnimationFrame() -- return the quad for the appropriate animation frame in 

end

function update() -- update timers used for changing animation frames

end
